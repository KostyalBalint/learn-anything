# Section metadata. Same shape as a subject _meta.yaml, minus summary.
# `order` places this section among the subject's pages (they share one ordering space).
title: Networking
order: 4
icon: network
